<UserControl
    x:Class="TinyUnrealPackerExtended.Views.LocresToolkitView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:TinyUnrealPackerExtended.Behaviors"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">
    <Border Style="{StaticResource CardStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition MinHeight="250" />
                <RowDefinition
                    Height="*"
                    MinHeight="220"
                    MaxHeight="180" />
                <RowDefinition Height="60" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!--  Drop Zone (input .locres/.csv)  -->
            <hc:DashedBorder Grid.Row="0" Style="{StaticResource AnimatedDashedDropZone}">
                <i:Interaction.Behaviors>
                    <behaviors:FileDropBehavior FileDropCommand="{Binding DropLocresFilesCommand}" />
                </i:Interaction.Behaviors>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <iconPacks:PackIconMaterial
                        Width="50"
                        Height="90"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource PrimaryButtonBackgroundBrush}"
                        Kind="FileDownloadOutline" />

                    <TextBlock
                        FontSize="20"
                        Foreground="{DynamicResource PlaceholderBrush}"
                        Text="{DynamicResource Locres.DropHint}"
                        TextAlignment="Center" />

                    <Button
                        Margin="0,14,0,0"
                        HorizontalAlignment="Center"
                        Command="{Binding BrowseLocresInputCommand}"
                        Content="{DynamicResource Locres.SelectFile}"
                        Style="{StaticResource SecondaryButtonStyle}" />

                    <TextBlock
                        Margin="0,6,0,0"
                        FontSize="12"
                        Foreground="{DynamicResource NavigationButtonDisabledForegroundBrush}"
                        Text="{DynamicResource Locres.FileTypes}"
                        TextAlignment="Center" />
                </StackPanel>
            </hc:DashedBorder>

            <!--  Drop Zone (original .locres)  -->
            <hc:DashedBorder Grid.Row="1" Margin="10,20,10,10">
                <i:Interaction.Behaviors>
                    <behaviors:FileDropBehavior FileDropCommand="{Binding DropOriginalLocresFilesCommand}" />
                </i:Interaction.Behaviors>
                <hc:DashedBorder.Style>
                    <Style BasedOn="{StaticResource AnimatedDashedDropZone}" TargetType="hc:DashedBorder">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCsvFileDropped}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </hc:DashedBorder.Style>

                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock
                        FontSize="16"
                        Foreground="{DynamicResource PlaceholderBrush}"
                        Text="{DynamicResource Locres.Original.DropHint}"
                        TextAlignment="Center" />

                    <Button
                        Margin="0,14,0,0"
                        Command="{Binding BrowseOriginalLocresCommand}"
                        Content="{DynamicResource Locres.Original.SelectFile}"
                        Style="{StaticResource SecondaryButtonStyle}" />

                    <TextBlock
                        Margin="0,6,0,0"
                        FontSize="12"
                        Foreground="{DynamicResource NavigationButtonDisabledForegroundBrush}"
                        Text="{DynamicResource Locres.Original.FileTypes}"
                        TextAlignment="Center" />
                </StackPanel>
            </hc:DashedBorder>

            <!--  Files area / progress  -->
            <Border
                Grid.Row="1"
                Margin="10,20,10,10"
                Background="{DynamicResource ItemHolderBackgroundBrush}"
                CornerRadius="6">
                <Border.Style>
                    <Style BasedOn="{StaticResource FileBorderStyle}" TargetType="Border">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsCsvFileDropped}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>

                <Grid>
                    <TextBlock
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        FontSize="20"
                        Foreground="{DynamicResource TextSecondaryBrush}"
                        Text="{DynamicResource Uploader.Empty}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLocresFileDropped}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <ProgressBar
                        Margin="20,31,20,32"
                        Background="{DynamicResource ProgressBarBackgroundBrush}"
                        Foreground="{DynamicResource ProgressBarFillBrush}"
                        IsIndeterminate="True">
                        <ProgressBar.Style>
                            <Style BasedOn="{StaticResource ProgressBarSuccess}" TargetType="ProgressBar">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLocresBusy}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ProgressBar.Style>
                    </ProgressBar>

                    <StackPanel Margin="10">
                        <ItemsControl ItemTemplate="{StaticResource FileItemTemplate}" ItemsSource="{Binding LocresFiles}">
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLocresBusy}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                        </ItemsControl>

                        <ItemsControl ItemTemplate="{StaticResource FileItemTemplate}" ItemsSource="{Binding OriginalLocresFiles}">
                            <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLocresBusy}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ItemsControl.Style>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </Border>

            <!--  Bottom buttons  -->
            <StackPanel
                Grid.Row="2"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Orientation="Horizontal">
                <Button
                    Command="{Binding CancelLocresCommand}"
                    Content="{DynamicResource Common.Cancel}"
                    Cursor="Hand"
                    Style="{StaticResource SecondaryButtonStyle}" />
                <Button
                    Command="{Binding ProcessLocresCommand}"
                    Content="{DynamicResource Locres.Process}"
                    Cursor="Hand"
                    Style="{StaticResource PrimaryButtonStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
